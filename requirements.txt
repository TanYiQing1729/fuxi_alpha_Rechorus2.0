# Minimal runtime requirements for reproducing the course project (ReChorus2.0 based)
# Tested on Windows + Python 3.12.

# Core
numpy>=1.26.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0

# Training / utils
tqdm>=4.66.0
PyYAML>=6.0.1

# Plotting
matplotlib>=3.8.0

# Pretty tables / optional logging
tabulate>=0.9.0

# PyTorch (CPU or CUDA wheel depends on your environment)
# 建议按官方指引安装与你机器匹配的 wheel：
#   https://pytorch.org/get-started/locally/
# 例如（Windows CPU）：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=1.12.0
